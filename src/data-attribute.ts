const base = "data-imp";

/**
 * The type of sensitive information that was redacted.
 */
export const redactionType = `${base}-rd`;

/**
 * The data attribute for section names.
 */
export const sectionName = `${base}-s`;
