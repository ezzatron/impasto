const base = "--imp";
const sectionContentIndentBase = `${base}-sc-i`;

/**
 * The CSS property for section content indent space count.
 */
export const sectionContentIndentSpaceCount = `${sectionContentIndentBase}-s`;

/**
 * The CSS property for section content indent tab count.
 */
export const sectionContentIndentTabCount = `${sectionContentIndentBase}-t`;
